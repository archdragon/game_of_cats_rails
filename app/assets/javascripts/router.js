// For more information see: http://emberjs.com/guides/routing/

Cat.Router.map(function() {
  // this.resource('posts');
  this.resource('about');
  this.resource('cat');
  this.resource('trainings');
  this.resource('missions');
  this.resource('regenerations');
});
